import { CreateWebinarForm } from "@/components/forms/create-webinar";

const CreateWebinarPage = () => {
  return (
    <div className="bg-accent flex min-h-screen w-full items-center justify-center px-4 py-12">
      <CreateWebinarForm />
    </div>
  );
};

export default CreateWebinarPage;
